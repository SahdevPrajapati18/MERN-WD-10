//Callback is a function can be passed in a function as a argument and those function which recieving the fucntion is called as higher order function 
//Inverison of Control That's why we don't pass the function we attach the function
//Callback hell
//Every function has a dedicated task
//Curring Approach
//Pyramid of Doom or Callback hell
//Because of that we move from callback -> promises if promise is fullfill we use .then and if promise is not fulfill we use .catch and both steps will also run .finaly that is called task completion message
